# Django Settings
DEBUG=False
SECRET_KEY=your-super-secret-key-change-this-in-production

# Database Settings
DB_NAME=survey_db
DB_USER=survey_user
DB_PASSWORD=changeme123

# Domain Settings (Update after DNS configuration)
# For Replit: use .replit.dev,.repl.co,.pike.replit.dev for wildcard matching
# For production: use your actual domain (wildcards must start with a dot in Django)
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com,www.your-domain.com

# Email Settings (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
